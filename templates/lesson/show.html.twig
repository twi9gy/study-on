{% extends 'base.html.twig' %}

{% block title %}{{ lesson.title }}/{{ lesson.course.title }}/StudyOn{% endblock %}

{% block body %}
    <h1>Урок. {{ lesson.title }}</h1>
    <p>Курс: <a href="{{ path('course_show', { 'id': lesson.course.id }) }}">{{ lesson.course.title }}</a></p>

    <a href="{{ path('lesson_edit', {'id': lesson.id}) }}">Редактировать</a>
    {{ include('lesson/_delete_form.html.twig') }}

    <p>{{ lesson.content }}</p>
{% endblock %}
